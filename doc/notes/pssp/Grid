#include "Dimension.h"

class Grid {

   class Vector {
   }

   class RField {
   }

   class KField {
   }

   void setUnitCell(const UnitCell& unitCell)
   void setDimensions(Grid::Vector);
   void allocate()
   void deallocate()
   void computeKSq()
   Grid::Vector computeKtoFFT(Grid::Vector)
   Grid::Vector computeKtoBZ(Grid::Vector)
   void computeMaxKabs()

   const KField& kSq()
   double maxKSq()
   
private:

   Grid::Vector  dimensions_
   Grid::KField  ksq_;
   double maxKSq_;

}
