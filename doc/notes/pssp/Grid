#include "Dimension.h"
#include "RField.h"
#include "KField.h"

class Grid {

   class Vector {
   }

   void setUnitCell(const UnitCell& unitCell)
   void setDimensions(Grid::Vector);
   void allocate()
   void deallocate()
   void computeKSq()
   Grid::Vector kToFFT(Grid::Vector)
   Grid::Vector kToBZ(Grid::Vector)
   void computeMaxKabs()

   const KField& kSq()
   double maxKSq()
   
private:

   Grid::Vector dimensions_
   Grid::KField ksq_;
   double maxKSq_;

}
